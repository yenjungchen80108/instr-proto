import { combineSlices } from "@reduxjs/toolkit";

// import instrEditor from "@/events/202501/instrEditor/store";

const rootReducer = combineSlices({
  // instrEditor,
  // add reducer
});

export default rootReducer;
